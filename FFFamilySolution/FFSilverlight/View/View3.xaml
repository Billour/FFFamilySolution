<UserControl x:Class="FFSilverlight.View.View3"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="VModel3"
    d:DesignHeight="300" d:DesignWidth="400">
    
    
    
    <Grid x:Name="LayoutRoot" Background="White">
        <StackPanel Orientation="Vertical">
            <TextBox x:Name="InputText" Margin="5" Width="200"/>
            <Button x:Name="Submit" Content="Submit" Command="{Binding SubmitCommand}" Margin="5"/>
            <TextBlock x:Name="DisplayText" Width="200" Text="{Binding Path=DisplayText}"></TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
